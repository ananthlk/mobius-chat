# =============================================================================
# Mobius Chat — copy to .env and adjust. Never commit .env.
# Or use mobius-config/.env (env_helper loads module first, then global).
# =============================================================================

# Queue
QUEUE_TYPE=memory
# REDIS_URL=redis://localhost:6379/0

# Live streaming: when worker runs separately (Redis), set so API subscribes to Redis for thinking/message stream
# CHAT_LIVE_STREAM=1

# Vertex AI (LLM + embeddings)
VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_LOCATION=us-central1
VERTEX_MODEL=gemini-2.5-flash
# Put GCP key in mobius-chat/credentials/ or mobius-config/credentials/; env_helper auto-finds *.json
# GOOGLE_APPLICATION_CREDENTIALS=credentials/your-service-account.json

# RAG: same DB as dbt CHAT_DATABASE_URL (published_rag_metadata)
# CHAT_RAG_DATABASE_URL=postgresql://user:password@host:5432/mobius_chat
# Get from GCP: Vertex AI → Vector Search → Index Endpoints → your endpoint → Deployed indexes (ID column)
# VERTEX_INDEX_ENDPOINT_ID=projects/YOUR_PROJECT/locations/us-central1/indexEndpoints/YOUR_ENDPOINT_ID
# VERTEX_DEPLOYED_INDEX_ID=your_deployed_index_id

# User auth - plug-and-play: proxy to Mobius-OS (recommended) or use mobius-user standalone
# Option A: Proxy to Mobius-OS (same users as extension) - set this:
# MOBIUS_OS_AUTH_URL=http://localhost:5001
# JWT_SECRET=<same-as-mobius-os>  # For token validation when proxying
# Option B: Standalone mobius-user (separate DB):
# USER_DATABASE_URL=postgresql://user:pass@localhost:5432/mobius_user

# Optional
# CHAT_LLM_PROVIDER=vertex
# CHAT_RAG_TOP_K=10
# API_BASE_URL=http://localhost:8000

# Debug: trace every key module/function call (worker log shows [trace] module.function entered)
# CHAT_DEBUG_TRACE=1
# DEBUG_TRACE=1
