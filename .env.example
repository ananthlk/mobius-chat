# =============================================================================
# Mobius Chat — copy to .env and adjust. Never commit .env.
# Or use mobius-config/.env (env_helper loads module first, then global).
# =============================================================================

# Queue
QUEUE_TYPE=memory
# REDIS_URL=redis://localhost:6379/0

# Vertex AI (LLM + embeddings)
VERTEX_PROJECT_ID=mobiusos-new
VERTEX_LOCATION=us-central1
VERTEX_MODEL=gemini-2.5-flash
# Put GCP key in mobius-chat/credentials/ or mobius-config/credentials/; env_helper auto-finds *.json
GOOGLE_APPLICATION_CREDENTIALS=credentials/mobiusos-new-090a058b63d9.json

# RAG: same DB as dbt CHAT_DATABASE_URL (published_rag_metadata)
CHAT_RAG_DATABASE_URL=postgresql://postgres:MobiusOS123$@34.59.175.121:5432/mobius_chat
# Get from GCP: Vertex AI → Vector Search → Index Endpoints → your endpoint → Deployed indexes (ID column)
# VERTEX_INDEX_ENDPOINT_ID=projects/mobiusos-new/locations/us-central1/indexEndpoints/YOUR_ENDPOINT_ID
# VERTEX_DEPLOYED_INDEX_ID=endpoint_mobius_chat_publi_1769989702095

# Optional
# CHAT_LLM_PROVIDER=vertex
# CHAT_RAG_TOP_K=10
# API_BASE_URL=http://localhost:8000

# Debug: trace every key module/function call (worker log shows [trace] module.function entered)
# CHAT_DEBUG_TRACE=1
# DEBUG_TRACE=1
