fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
PyJWT>=2.8.0
# mobius-user: shared auth (install from sibling: pip install -e ../mobius-user)
pydantic>=2.0.0
python-dotenv>=1.0.0
# Optional: for QUEUE_TYPE=redis (API and worker in separate processes)
redis>=5.0.0
# Optional: for LLM_PROVIDER=vertex (Vertex AI / Gemini)
google-cloud-aiplatform>=1.38.0
# For CHAT_RAG_DATABASE_URL (published_rag_metadata Postgres)
psycopg2-binary>=2.9.0
# Repair malformed LLM JSON (trailing commas, missing quotes, etc.) before LLM repair
json-repair>=0.55.0
# Chat bot script (config and questions YAML)
PyYAML>=6.0.0
# Tests
pytest>=7.0.0
# Payer normalization config (config/payer_normalization.yaml)
PyYAML>=6.0.0